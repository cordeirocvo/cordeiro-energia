export const pushToN8N = async (payload: any, eventType: string = 'UPDATE') => {
  // Desenvolvimento do N8N paralizado conforme solicitação.
  // console.log(`Disparo N8N ignorado para evento: ${eventType}`);
  return;
};
